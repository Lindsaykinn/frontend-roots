const initialState = {
  families: [],
  loading: true
}

const familiesReducer = (state=initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}
 
export default familiesReducer;